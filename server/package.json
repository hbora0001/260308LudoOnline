{
  "name": "ludo-online-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "build": "cd ../client && npm run build && rm -rf ../server/dist && cp -r dist ../server/ || (cd ../client && rmdir /s /q dist 2>nul & xcopy dist ..\\server\\dist /E /I /H /Y)"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
